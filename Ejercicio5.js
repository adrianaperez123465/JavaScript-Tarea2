/*Realiza una función la cual recibe por parámetro un número lo eleve al cuadrado.  */

let number = 7;

function cuadrado(number) {
   result = number*number;  
   console.log("El resultado es: "+ result);
}

cuadrado (number)